#include <stdio.h>
int main (void){
	int sr, fr, yr, dr;
	yr = 0;
	printf("Enter start year: ");
	scanf("%d", &sr);
	printf("\n Enter finish year: ");
	scanf("%d", &fr);
	dr = (fr - sr) / 4;
	if (sr % 4 = 0){
		while(
	
